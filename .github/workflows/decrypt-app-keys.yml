name: Decrypt App Keys

on:
  workflow_dispatch:

env:
  DEPLOYMENT_NAME: Decrypt App Keys # TODO: update to deployment name
  key: ${{ secrets.key }}

jobs:
  apigee-window-artifacts:
    name: Decrypt App Keys
    runs-on: ubuntu-latest

    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
      - name: Set Git identity
        run: |
          git config --global user.email "rajeshjanapati533@gmail.com"
          git config --global user.name "pavansirasanambedu"

      - name: Check out repository code
        uses: actions/checkout@v3
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Decrypt App Keys
        shell: pwsh
        run: |
          $env:token = "${{ secrets.GIT_TOKEN }}"
          $env:key = "${{ secrets.key }}"
          
          $env:sourcegithubUsername = "pavansirasanambedu"
          $env:sourceRepo = "common-encrypt-decrypt"
          $env:sourcebranchname = "encrypt/appkeys"
          $env:sourcefilePath = "encrypt/encrypt-appkeys.json"

          $env:targetgithubUsername = "pavansirasanambedu"
          $env:repositoryName = "common-encrypt-decrypt"
          $env:targetBranchName = "decrypt/appkeys" 
          $env:targetFilePath = "decrypt/decrypt-appkeys.json" 

          # $env:fieldsToDecrypt = @("consumerKey", "consumerSecret")
          
          Invoke-Pester ./decrypt-app-keys.ps1 -Passthru
